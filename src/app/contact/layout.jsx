import React from "react";

export const metadata = {
  title: "Contact",
  description: "contacter nous description",
};

export default function layout({ children }) {
  return <main>{children}</main>;
}
